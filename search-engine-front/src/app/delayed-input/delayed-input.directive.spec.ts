import { DelayedInputDirective } from './delayed-input.directive';

describe('DelayedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayedInputDirective();
    expect(directive).toBeTruthy();
  });
});
